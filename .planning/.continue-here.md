---
context: default
phase: sheet-layout-and-docs
status: in-progress
last_updated: 2026-09-15
---

# Arbeitsstand

Kurzer Wiedereinstieg. Verbindlich ist `AGENTS.md`, nicht diese Datei.

## Branch und Arbeitsverzeichnis

- Arbeitsverzeichnis: `C:\Prv\pnp\RogueTraider\Character sheet\Wissensdatenbank`
  (keine Worktree, direkt im Checkout).
- Branch: `improve/sheet-layout`, Remote `origin/improve/sheet-layout`, Basis `main`.
- Letzte Commits: „Show crosses in advancement circles", „Sync character stats
  across sheet pages", „Improve character and ship sheet layouts".

## Laufende Arbeit

**1. Anweisungsbereinigung (dieser Durchgang, abgeschlossen und gestaged).**
`AGENTS.md` ist neu und der verbindliche Einstieg: Rangfolge, Zuständigkeiten,
Testpflichten. Aktualisiert: beide Skills unter `.claude/skills/`, der Test-Hook
in `.claude/settings.json` (führt jetzt nur noch die Tests der betroffenen App
aus), `docs/charakterbogen-feld-anforderungen.md`, `docs/sheet-calibration.md`,
`README.md`. Specs, Pläne und der Brainstorm tragen einen Historien-Hinweis.
`docs/prompt-seite-2.md` liegt jetzt unter `docs/archive/`;
`00-WIE-ES-FUNKTIONIERT.md` wurde entfernt. `.claude/` und `.mcp.json` sind ab
jetzt versioniert. Noch nicht committet.

**2. Offene Sheet-Änderungen (nicht aus diesem Durchgang, unverändert
stehengelassen):** `sheets/static/sheets/sheet-viewer.css`,
`tests/e2e/test_continuous_sheet_layout.py` und zwei neu aufgenommene
Vergleichsbilder unter `tests/visual/`. Sie gehören zur Kreuz-Darstellung der
Checkboxen und sind weder gestaged noch committet.

## Status

| Prüfung | Ergebnis |
|---|---|
| `.venv/Scripts/python.exe -m pytest -q` | 432 bestanden (~4 min), 2026-09-15 |
| `.venv/Scripts/python.exe -m sheets.layout --check` | „Layouts are current." |
| Build / Deployment | in diesem Durchgang nicht angefasst; Docker-Rundlauf siehe `README.md` |

## Verbleibende manuelle bzw. nutzergesteuerte Arbeit

1. **Sichtprüfung der Bögen** nach der Abnahmeliste in `README.md` — jede
   gedruckte Checkbox und Markierung gegen ihr Overlay, über die Zoomstufen
   30–100 %. Mobile/Touch ist bewusst außerhalb des Projektumfangs
   (Desktop ab 1024 px, siehe `.claude/skills/project-conventions/SKILL.md`).
2. Im realen Reverse Proxy `X-Real-IP` aus der tatsächlichen Peer-Adresse
   überschreiben (nie Browserwerte anhängen oder beibehalten) und die
   bestehende Firewallgrenze zum Portal verifizieren.
3. HSTS erst aktivieren, wenn der echte HTTPS-Pfad über den Reverse Proxy
   nachgewiesen ist.
4. Git-Aktionen liegen beim Nutzer: committen, pushen und mergen entscheidet
   der Projektinhaber.
5. Die in `docs/checkbox-row-mapping.md` beschriebene Zuordnung der
   Fertigkeitskästchen klären — sie braucht eine Datenmigration, keine
   Geometriekorrektur.

Für jeden Erststart bzw. jedes frische Produktions-Deployment bleibt der
dokumentierte explizite Schritt `manage.py migrate` verbindlich.

## Nicht anfassen

- `Notizbuch öffnen.onetoc2` (OneNote; im Wurzelverzeichnis getrackt und lokal
  geändert) und `graphify-out/` — fremde Artefakte, nie bearbeiten, nie stagen.
- `.env` — für Agenten per Hook gesperrt; `.env.example` ist die Vorlage.

## Sicherheitslektion

Nie Implementierer oder Reviewer mit `isolation:"worktree"` in eine andere
Worktree dispatchen. Dieser Modus führte im Task-3-Vorfall zu einer anderen
Worktree und einem unzulässigen Sandbox-Umgehungsversuch. Agents müssen in der
bereits vorbereiteten Ziel-Worktree arbeiten.
